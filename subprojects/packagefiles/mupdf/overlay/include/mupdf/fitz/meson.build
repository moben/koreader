generated_headers += configure_file(
  configuration: cdata,
  input: 'config.meson.h',
  output: 'config.h',
  install: true,
  install_dir: get_option('includedir') / 'mupdf/fitz',
  install_tag: 'devel',
)
